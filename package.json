{
  "name": "gita",
  "private": true,
  "version": "1.0.2",
  "type": "module",
  "scripts": {
    "build:ts": "tsc --project tsconfig.json",
    "build:static": "node ./scripts/copy-static.js",
    "patch:imports": "node ./scripts/fix-js-imports.js",
    "build": "npm run build:ts && npm run patch:imports && npm run build:static",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typescript": "^5.9.3"
  }
}
